# Журнал починки тупиков и правок контента

**Дата:** 20 сентября, 00:20
**Итог:** достижимость **455 из 455** (было 446 по валидатору и 440 по факту), проверок — **0 ошибок**, предупреждение одно (дефект книги в узле 445).

## 1. Пять потерянных отметок → `overrides.json` → `effects` / `replace_effects`

| Отметка | Где теперь ставится | Обоснование |
|---|---|---|
| 13 | **481** (дом бобра Древогрыза) | проверяется в квадрате 2А, фраза постановки потеряна |
| 24 | **400** (двор замка Белой Колдуньи, первое посещение) | проверяется в квадрате 3А |
| 27 | **148** вместо 29 (падение в шахту гномов, Тарарам и Тихоня) | в книге ошибка нумерации: 29 проверяется в 4Б и ставится в 530 |
| 31 | **278** («Моё имя – Маэния») | проверяется в квадрате 4Г |
| 34 | **415** (первое событие квадрата 5В, вишнёвый сад) | аналог: узел 519 ставит отметку 33 для квадрата 5Б |

Отметки ставится/проверяется: 43 / 40. Проверяются-но-не-ставятся: **нет**.

## 2. Шесть связок для осиротевших узлов → `overrides.json` → `choices` / `square_branches`

| Тупик | Связка | Качество |
|---|---|---|
| 520 «Отказываясь от пути добродетели» | новая кнопка «Отказаться от прощения» в **300** (сцена прощения Аслана) | чистая, парный выбор к 392 |
| 214 бой с осьминогом мечом | новая кнопка «Ударить мечом» в **456** (щупальце хватает за руку) | чистая; меч введён в 459/477/480, вход в пещеру — 518 |
| 553 прятанье в ветвях дуба | новая кнопка «Забраться на дуб» в **318** (великаны гонятся) | чистая, третья попытка спрятаться рядом с 237/292 |
| 295 → 380/381 второй визит к ледяному озерцу | новая кнопка «Взглянуть на самоцветы» в **505** | средняя: 380/381 частично дублируют сцены выбора камней 116/126 |
| 250 «наученный Слепышом, обхожу сад» | новая ветка квадрата **5В** по отметке 35 (вставлена перед «Иначе») | средняя: вариант-двойник узла 157 |
| 546 белка указывает тропинку | новая кнопка «Спросить дорогу у белки» в **248** | слабая: сцена-двойник 291, выход в другой квадрат (2А вместо 1А) |

Все шесть помечены в контенте флагом `editorial: true` и несут `editorial_note` с обоснованием.
Порядок веток в квадрате 5В: условные (34, 35) → безусловное «Иначе» — иначе «Иначе» перехватывало бы игрока с отметкой 35.

## 3. Правка склейки прозы

`build_content.py`, `join_sentences`: из правила «не ставить пробел» убраны тире (`–`, `—`, `-`).
Было `–Если`, стало `– Если` в 10 узлах (103, 158, 199, 200, 216, 343, 386, 441, 481, 552). Других изменений прозы нет — 0 случаев «–Если».

## 4. Подписи

- Правки аудита: **17** подписей (13 объективных + 187, 205, 503, 511) → `labels_out/labels_fixes_done.json`.
- Квадраты: 45 подписей → `labels_out/labels_squares_done.json`; у квадрата 5В ветка «Иначе» переехала на idx 2 (вставлена условная ветка).
- Снято 8 подписей, висевших на ветвях броска (160×2, 255, 370, 391×2, 445×2) → `labels_out/_retired_dice_labels.json` (маска `*_done.json` его не читает, в игру он не попадает).
- Всего кнопок 391 (386 книжных + 5 редакторских), без подписи — 0.

## 5. Тронутые файлы

| Файл | Что |
|---|---|
| `content/overrides.json` | 5 отметок, 6 связок, обоснования |
| `scripts/build_content.py` | пробел после тире; применение `effects`/`replace_effects`/`choices`/`square_branches`; вставка условной ветки перед «Иначе»; редакторские кнопки не попадают в задания на разметку |
| `scripts/merge_labels.py` | не перезаписывает подписи с флагом `editorial` |
| `content/labels_out/labels_fixes_done.json` | 17 правок подписей |
| `content/labels_out/labels_squares_done.json` | сдвиг idx у квадрата 5В |
| `content/labels_out/_retired_dice_labels.json` | 8 снятых подписей |

Пересборка: `python3 scripts/build_content.py && python3 scripts/merge_labels.py && python3 scripts/validate_content.py`.

## 6. Осталось

1. **Узел 445** — в книге бросок без навыка (задокументировано в `REPORT.md` §3.2). Трактуем как бросок без модификатора.
2. **Условные кнопки в движке:** у 17 узлов есть `checks`. Пока отметка не стоит, кнопку надо **скрывать**, а не показывать серой — иначе название раскрывает скрытое требование (критерий §10.2 «нет спойлеров в названиях недоступных кнопок»).
3. **6 редакторских связок** стоит пройти в игре: связки по 250, 295/380/381 и 546 — варианты-двойники, возможен повтор сцены при повторном проходе.
4. Частота подписи «Продолжить путь» — 22 раза (из них 3 поправлены); остальное — допустимая унификация.
