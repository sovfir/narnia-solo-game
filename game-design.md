# «Колдунья и Книга заклинаний» — мобильная web-игра

**Дизайн-документ: экраны, механики, техническая архитектура и пошаговый план разработки**

> Основано на `rules.md` (механика и смысл) и `texts.md` (полный текст книги: 24 квадрата карты, 455 параграфов событий, 15 финалов).
> Стек: TypeScript + Vite + three.js + PWA.

---

## Содержание

1. [Анализ исходного материала](#1-анализ-исходного-материала)
2. [Продуктовая концепция](#2-продуктовая-концепция)
3. [UX-принципы мобильной версии](#3-ux-принципы-мобильной-версии)
4. [Карта экранов](#4-карта-экранов)
5. [Подробное описание экранов](#5-подробное-описание-экранов)
6. [Игровые механики](#6-игровые-механики)
7. [Контент-пайплайн и модель данных](#7-контент-пайплайн-и-модель-данных)
8. [Техническая архитектура](#8-техническая-архитектура)
9. [Пошаговый план разработки](#9-пошаговый-план-разработки)
10. [Тестирование и приёмка](#10-тестирование-и-приёмка)
11. [Риски и юридический вопрос](#11-риски-и-юридический-вопрос)
12. [Объём MVP и версия 2.0](#12-объём-mvp-и-версия-20)

---

## 1. Анализ исходного материала

### 1.1 Что есть на входе

| Файл | Содержимое | Объём |
|---|---|---|
| `rules.md` | Правила, смысл, статистика, памятка ведущего | 263 строки |
| `texts.md` | Все тексты: правила, создание героя, пролог, 24 квадрата, 455 событий, приложения | 4270 строк |

### 1.2 Структурные факты, определяющие дизайн

| Метрика | Значение | Следствие для игры |
|---|---|---|
| Квадратов карты | 24 (`1А–6Г`, 6 столбцов × 4 ряда) | Экран карты — сетка 6×4, 8 направлений движения |
| Параграфов событий | 455 (`100–554`, без пропусков) | Основной контент; нужен парсер и валидатор |
| Всего исходящих ссылок | 533 | Граф переходов; ~1.2 ссылки на узел |
| Параграфов с ≥2 вариантами | 196 | Больше трети узлов — реальный выбор |
| «Кубиковых» проверок | 72 | Экран броска — ключевой элемент, а не редкость |
| Переходов на карту | 152 | Карта — постоянный спутник, а не эпизод |
| Мёртвых концов | 15 | Экран финала + галерея концовок |
| Осиротевших параграфов | 13 (`214, 248, 250, 283, 290, 292, 295, 317, 494, 497, 520, 546, 553`) | Из них `317` и `494` — точки входа; остальные достижимы только из карты |
| Загрузка навыков | Энергия 43 · Проницательность 19 · Внутренняя сила 15 · Хватка 12 · Изворотливость 7 · Красноречие 7 | Энергия — самый востребованный навык, нужен акцент в UI |

### 1.3 Инвентарь механик (что обязано быть в игре)

1. **Персонаж:** 6 навыков, сумма = 6, максимум +3 на навык, неиспользованный навык = −2.
2. **Проверка:** `случайное 2–12 + навык`, зажимается в `[2, 12]`, сравнивается с диапазоном из текста.
3. **Отметки путешествия:** чекбоксы, реально используются `1–45`; ставятся («Поставь отметку N») и проверяются («проверь Ключ N»).
4. **Карта:** 24 квадрата, движение «Вперёд» в том числе по диагонали.
5. **Предметы и сокровища:** влияют на навыки, часть — ключи к финалу.
6. **Временные модификаторы:** «вычитай 1 из Энергии следующие два раза».
7. **Финалы:** 1 победа (`235`), 1 падение (`526`), 13 гибелей/провалов.
8. **Моральный слой:** Внутренняя сила как шкала добродетели; искупление (`300`, `455`) — путь к книге остаётся открыт.

### 1.4 Ключевые сюжетные опоры (для подсказок и целей)

- Цель: найти книгу «Руны благодатного скипетра» и принести Аслану в Кэр-Паравел.
- Нужен **ключ с Глазом Осьминога** (отметка `6`), цена — **Северный Гранат** (отметка `38`).
- Ложные ключи: Змеи (`19`), Луны (`22`), Розы (`23`).
- Путь: Карга → Гранат → ключ `6` → книга (`392`) → Кэр-Паравел → `127` → `227` → `235`.

---

## 2. Продуктовая концепция

### 2.1 Одной фразой

**Книга-игра, которая ведёт тебя сама:** мобильное web-приложение, где бумажный квест оживает — карта подсвечивает путь, кубики стучат по столу, а твои решения складываются в моральный дневник.

### 2.2 Целевой опыт

| Параметр | Решение |
|---|---|
| Платформа | Мобильный web (PWA), устанавливается на домашний экран |
| Ориентация | Портретная, одна рука |
| Длительность сессии | 5–15 минут (одна «сцена» = 3–8 узлов) |
| Длительность партии | 40–90 минут до финала |
| Аудитория | 14+, любители книг-игр, фанаты Нарнии |
| Тон | Пахучая бумага, тёплый свет, сдержанная кинематографичность |

### 2.3 Основной цикл (core loop)

```
Пролог
  → Карта: выбор соседнего квадрата («Вперёд»)
  → Описание местности: что ты видишь
  → Событие: текст сцены
  → Выбор / проверка навыка / проверка отметки
  → Последствие: переход, предмет, рана, отметка
  → … повторить …
  → Финал (победа / падение / пробуждение в Англии)
```

### 2.4 Чем цифровая версия лучше бумажной

1. **Кубики не нужно искать** — бросок в один тап, с анимацией и честным RNG.
2. **Карта сама знает, где ты** — не надо рисовать путь карандашом.
3. **Отметки не забываются** — движок сам ставит и проверяет ключи.
4. **Ссылки не теряются** — переход по кнопке, а не поиск номера страницы.
5. **Дневник решений** — видно, какие поступки подняли или опустили Внутреннюю силу.
6. **Автосохранение** — закрыл вкладку, вернулся и продолжил.

### 2.5 Что мы НЕ меняем

- Тексты остаются авторскими, без переписывания.
- Формула проверки, диапазоны, отметки и структура переходов — ровно как в книге.
- 15 финалов сохраняются полностью.

---

## 3. UX-принципы мобильной версии

| Принцип | Реализация |
|---|---|
| Одна рука | Все основные действия — в нижней трети экрана (thumb zone) |
| Крупные цели | Тап-зоны не меньше 48×48 px, отступ между кнопками ≥ 12 px |
| Читаемость | Кегль 18–20 px, интерлиньяж 1.6, максимум ~65 символов в строке |
| Один фокус | На экране одно главное действие; второстепенные — в шторках |
| Никаких тупиков | Если выбор недоступен — кнопка видна, но неактивна и объясняет почему |
| Мгновенный отклик | Любой тап даёт визуальный отклик ≤ 100 мс; бросок — анимация ≤ 1.5 с |
| Автосохранение | Состояние пишется после каждого узла, без диалогов «сохранить?» |
| Офлайн | PWA-кэш, игра работает без сети после первой загрузки |
| Тёмная тема | Обязательна: ночное чтение + экономия батареи на OLED |
| Уважение к глазам | Плавные переходы, без резких вспышек; вибрация отключаема |
| Доступность | Динамический шрифт, поддержка screen reader на текстовых экранах, контраст ≥ 4.5:1 |

### 3.1 Визуальный язык

- **Палитра:** пергамент `#F4E9D8`, чернила `#2B2118`, зелёный Нарнии `#3E5C4B`, золото Аслана `#C9A227`, тревога `#8C2F2F`, ночной режим `#14110E` / `#E8DCC8`.
- **Шрифты:** заголовки — антиква с высоким контрастом; текст — гуманистическая антиква с хорошим кириллическим начертанием. Кегль заголовков не менее 24 px.
- **Фактуры:** лёгкая бумажная зернистость (CSS-шум, не картинкой — экономия трафика).
- **Иллюстрации:** стилизованные виньетки в духе гравюр; в MVP — CSS-градиенты и SVG, в v2 — three.js-сцены.

---

## 4. Карта экранов

```
S1 Splash / Загрузка
 └─→ S2 Главное меню
      ├─→ S3 Новая игра → S4 Создание героя → S5 Пролог → S8 Игра
      ├─→ S6 Продолжить (слоты сохранений)
      ├─→ S7 Галерея концовок
      └─→ S14 Настройки

S8 Игра (основной экран)
 ├─→ S9 Шторка выбора (если вариантов > 3)
 ├─→ S10 Экран броска кубиков (оверлей)
 ├─→ S11 Карта (полноэкранная)
 ├─→ S12 Лист персонажа (навыки, предметы)
 ├─→ S13 Дневник решений
 └─→ S15 Финал

S15 Финал → S7 Галерея концовок → S2 Главное меню
```

**Всего 15 экранов.** Ниже — детальное описание каждого.

---

## 5. Подробное описание экранов

### S1. Splash / Загрузка

**Назначение:** мгновенный старт, прогрев ассетов, инициализация парсера контента.

- **Композиция:** по центру — золотой лев на пергаменте (SVG), под ним тонкая полоса прогресса.
- **Состояния:**
  - *Первый запуск:* «Распаковываем книгу…» (парсинг `texts.md` в JSON, ~200 мс).
  - *Повторный запуск:* мгновенно, контент из IndexedDB.
- **Переход:** через 800 мс или по готовности → S2. Если есть незакрытая партия — кнопка «Продолжить» прямо здесь.
- **Технически:** здесь же прогреваются шрифты и three.js-контекст (offscreen), чтобы карта открылась без задержки.

---

### S2. Главное меню

**Назначение:** точка входа во все режимы.

**Зоны (сверху вниз):**
1. Логотип: «Колдунья и Книга заклинаний» + подзаголовок «Нарния · соло-квест».
2. **Продолжить** — крупная кнопка; показывает: номер параграфа, квадрат карты, мини-иконку героя.
3. **Новая игра** → S4.
4. **Галерея концовок** (`12 / 15`) → S7.
5. **Настройки** (иконка-шестерёнка в правом верхнем углу) → S14.

**Состояния:**
- Нет сохранений → «Продолжить» неактивна, подпись «Начни приключение».
- Есть сохранение → показывается превью: «📍 Квадрат 3В · ⚔️ Энергия +1 · ✅ 7 отметок».

**Микроанимация:** при появлении кнопки слегка «проявляются», как чернила на бумаге.

---

### S3. *(пропущен в нумерации — зарезервировано под онбординг)*

При необходимости: 3 свайп-слайда «Как играть» (навыки → кубики → карта). Показывается один раз, до создания первого героя.

---

### S4. Создание героя

**Назначение:** выбрать готового Робина или собрать своего.

**Два таба:**

**Таб «Готовый герой»**
- Карточка Робина Трэверстока: имя, «5 пенсов в кармане купальника», все навыки `+1`.
- Кнопка «Начать» — сразу в S5 (перед Прологом показывается «Предисловие» отдельной сценой).

**Таб «Свой герой»**
- Поле имени (опционально, по умолчанию «Робин Трэверсток»).
- Шесть строк навыков. В каждой: название, иконка, смысл в одну строку, `−` / значение / `+`.
- **Счётчик распределения:** «Распределено 6 из 6». Пока сумма ≠ 6 — кнопка «Начать» заблокирована.
- **Валидация:**
  - каждый навык от `−2` до `+3`;
  - сумма ровно `6`;
  - при `+3` кнопка `+` гаснет с подсказкой «максимум +3».
- **Подсказка-помощник:** «Энергия пригодится чаще всего — в 43 сценах».
- Кнопка «Сбросить» и кнопка «Случайно» (равномерно разложить 6 очков).

**Смыслы навыков (тултип при долгом тапе):**

| Навык | Когда применяется |
|---|---|
| Хватка | драка, физическое противостояние |
| Изворотливость | скрытность, кража, отмычки, выпутаться из верёвок |
| Энергия | бег, плавание, погоня, лазание, охота, езда верхом |
| Красноречие | разговор, добыча сведений у разумных существ |
| Проницательность | наблюдение, исследование, замечать скрытое |
| Внутренняя сила | мера добродетели и сопротивления злу |

---

### S5. Пролог и Предисловие

**Назначение:** ввести в мир, дать задание Аслана.

- Полноэкранный текст на пергаменте, вертикальный скролл.
- Текст подаётся **порциями** (2–4 абзаца), внизу — «Дальше».
- Между порциями — лёгкая смена фона (пляж → море → зал Кэр-Паравела).
- **Ключевая сцена:** Аслан даёт задание и магическую песню.
  - Песня выделяется особым блоком и **сохраняется в инвентарь** как предмет «Магическая песня»:
    > «Прочь отыди, Дух тлетворный, / Я не тронут Силой чёрной.»
- **Выбор:** «Уже был в Нарнии раньше?» → `494` иначе `317` (как в книге).
- **Переход:** в S8 на узле `317` / `494` → `181` → `155` → карта.

---

### S8. Игра — основной экран

**Назначение:** 90% времени игрока.

**Вертикальная композиция (сверху вниз):**

1. **Статус-бар (липкий, 56 px)**
   - Слева: `📍 Квадрат 3В` (тап → S11 карта).
   - В центре: название навыков-индикаторов — компактные пилюли с текущими уровнями (`Хватка +1`, `Энергия +1`).
   - Справа: иконка «Лист персонажа» (тап → S12), иконка «Дневник» (тап → S13).
   - В правом нижнем углу статус-бара — маленький индикатор автосохранения (галочка, гаснет через секунду).

2. **Заголовок сцены**
   - `Параграф 392` + тип узла (`Событие` / `Описание местности`).
   - Кнопка «?» — контекстная подсказка без спойлеров.

3. **Текст сцены (скроллируемый)**
   - Кегль 19 px, интерлиньяж 1.62.
   - Диалоги — с отступом и курсивом; ремарки — приглушённым цветом.
   - **Маркеры изменений** подсвечиваются прямо в тексте: изменение навыка (зелёная/красная пилюля), получение отметки (иконка ✅), предмет (иконка 🎒).
   - Если это «Описание местности» — текст сжимается в карточку-локацию с иллюстрацией-заголовком.

4. **Блок последствий (если есть)**
   - Появляется анимированно после текста: «Внутренняя сила +1», «Получена отметка 6», «Получен предмет: Ключ с Глазом Осьминога».
   - Значения летят к статус-бару (motion path), затем растворяются.

5. **Панель действий (липкая, низ экрана)**
   - 1–4 кнопки, крупные, на всю ширину, с номером слева: `1. Спеть песню → 146`.
   - Если вариантов больше 4 — кнопка «Ещё варианты» открывает S9.
   - Если узел требует проверки — вместо вариантов одна кнопка **«Бросить кубики»** (S10).
   - Если узел — «Вперёд» — кнопка **«Вперёд»**, открывающая S11 в режиме выбора соседнего квадрата.
   - Если узел — финал — кнопка **«Завершить»** → S15.

**Состояния и правила:**
- **Недоступный вариант:** кнопка показана, но приглушена; по тапу — короткая причина («Нужен ключ с Глазом Осьминога») без раскрытия сюжета.
- **Автоскролл:** новый узел всегда открывается с прокруткой вверх.
- **Анимация перехода между узлами:** 220 мс, сдвиг влево + лёгкое затемнение (имитация перелистывания).
- **Возврат назад:** стрелка «‹» в заголовке — откат на один узел (см. §6.11).

---

### S9. Шторка выбора

- Bottom sheet с 5–8 вариантами.
- Каждый вариант: номер, короткая подпись, целевой узел.
- Свайп вниз или тап по фону — закрыть.
- Нужна редко (в книге >4 вариантов почти нет), но обязана существовать.

---

### S10. Экран броска кубиков

**Назначение:** сердце игры. 72 проверки в тексте.

**Композиция:**
- Затемнение фона, в центре — деревянная подставка с двумя кубиками.
- Над кубиками: «Выбери число и добавь свою Энергию».
- **Анимация:** кубики подбрасываются (физика на three.js — см. §8.3), стучат, останавливаются. Длительность 0.9–1.4 с (настраивается).
- **Раскрытие результата по шагам** (важно для доверия к честности):
  1. Выпало на кубиках: `4` и `3`.
  2. Сумма: `7`.
  3. Прибавь Энергию `+1` → **`8`**.
  4. Зажимаем в диапазон `[2, 12]` → `8` (если сработало — показать сноску «ограничено»).
  5. Диапазон из текста: `От 7 до 12` → **переход на 112**.
- **Кнопка «Бросить»** — крупная, по центру; до броска она единственная активная.
- **После броска** — кнопка «Продолжить → 112».
- **Хаптика:** лёгкая вибрация на старте, двойная — на результате.

**Настройки броска (S14):**
- «Бросать кубики вручную» (по умолчанию) / «Показывать результат сразу» (для быстрого прохождения).
- Звук кубиков вкл/выкл.

---

### S11. Карта

**Назначение:** навигация по 24 квадратам.

**Композиция:**
- Полноэкранная сцена three.js: стилизованная объёмная карта-сетка 6×4.
- **Ряды:** `А`, `Б`, `В`, `Г`; **столбцы:** `1–6`.
- Текущий квадрат подсвечен золотым, доступные для перехода (8 соседей, включая диагонали) — пульсируют зелёным.
- Остальные квадраты — приглушённые, с микро-иконкой рельефа (лес, горы, река, замок, море).
- Названия локаций из книги: Кэр-Паравел, Фонарная пустошь, Каменный Стол, замок Белой Колдуньи, вишнёвый сад, Вересковые горы и т.д.
- **Панель снизу:** название текущего квадрата + краткое «что ты видишь» (первые 1–2 строки описания) + кнопка «Здесь» / «Идти сюда».
- **Жесты:** перетаскивание — поворот камеры, щипок — зум, двойной тап — сброс.

**Режимы:**
1. **Просмотр** — открыта из статус-бара, ничего не выбирает.
2. **Выбор направления** — открыта по кнопке «Вперёд»; соседние квадраты активны, тап по соседу → чтение «Описания местности» и следующий параграф.

**Фолбэк:** при слабом устройстве или отключённом WebGL — 2D-сетка на SVG/CSS с той же логикой (см. §8.4).

---

### S12. Лист персонажа

- **Шапка:** имя героя, тип (готовый/свой), пройдено узлов, время партии.
- **Навыки:** 6 строк с иконками, текущим уровнем и базовым значением. Временные модификаторы (например, «−1 к Энергии: осталось 2 раза») показываются отдельной плашкой с бейджем.
- **Предметы и сокровища:** карточки с названием, иконкой и описанием; ключи подсвечены отдельной секцией «Ключи».
- **Отметки путешествия:** сетка чекбоксов `1–45`. Активные — залиты, неактивные — контурные. Тап по активной показывает, где она была поставлена («Отметка 6 · параграф 236 · Ключ с Глазом Осьминога»).
- **Внутренняя сила:** отдельная шкала с пояснением «мера добродетели», история изменений (тап → S13).

---

### S13. Дневник решений

**Назначение:** сделать моральный слой видимым (это главная ценность книги, по §8 `rules.md`).

- Лента записей в обратном хронологическом порядке:
  - `📍 3В · «Ты отказался от соблазна» · Внутренняя сила +1`
  - `📍 5В · «Ты съел вишни» · Внутренняя сила −1`
  - `📍 2Г · «Получена отметка 16»`
- Фильтры: «Всё», «Поступки», «Предметы», «Отметки».
- Внизу — счётчик: «3 добрых поступка · 1 ошибка».
- **Важно:** дневник не меняет правил и не наказывает; он только показывает то, что уже произошло.

---

### S14. Настройки

| Группа | Пункты |
|---|---|
| Чтение | Размер шрифта (4 ступени), тема (пергамент / ночь / сепия), межстрочный интервал |
| Кубики | Анимация броска, звук, хаптика, «показывать результат сразу» |
| Игра | Подтверждение выхода, автосохранение (вкл, всегда), сброс партии |
| Служебное | Управление сохранениями (3 слота), экспорт/импорт сохранения в JSON, версия контента |

---

### S15. Финал

**Назначение:** достойно закрыть партию в любом из 15 исходов.

**Три визуальных тональности:**

| Тип | Узлы | Оформление |
|---|---|---|
| **Победа** | `235` | Золото, тёплый свет, пир, Аслан; крупная надпись «ТРИУМФ» |
| **Падение** | `526` | Холодные тона, гаснущий свет, «Ты уступил тёмной стороне» |
| **Гибель/провал** | `104, 125, 128, 137, 182, 221, 243, 301, 309, 426, 480, 516, 542` | Мягкое «пробуждение в Англии»; без жестокости, в тоне Льюиса |

**Содержимое экрана:**
- Финальный текст (полностью, как в книге).
- Сводка партии: время, пройдено узлов, квадратов, отметок, ключей, итоговая Внутренняя сила.
- **Разбор пути:** «До победы не хватило одного: ключ с Глазом Осьминога».
- Кнопки: «Начать заново» (новый герой), «Вернуться в меню», «Галерея концовок».

---

### S7. Галерея концовок

- Сетка из 15 карточек. Открытые — с цветной иллюстрацией и названием; закрытые — силуэт с «?».
- Тап по открытой — краткое описание исхода и статистика (сколько раз достигнут).
- Прогресс: `4 / 15`.
- Хранится между партиями в IndexedDB.

---

## 6. Игровые механики

### 6.1 Создание героя

```
Готовый герой: все 6 навыков = +1 (сумма 6).
Свой герой:    сумма навыков = 6; каждый навык в диапазоне [−2; +3].
               Неиспользованный навык записывается как −2.
```

Движок хранит и **базовое**, и **текущее** значение: базовое не меняется, текущее меняется предметами, ранами и поступками.

### 6.2 Проверка навыка

```ts
function check(rawDice: number, skill: number): number {
  return Math.min(12, Math.max(2, rawDice + skill));
}
```

- `rawDice` — сумма двух d6 (2–12).
- Результат сравнивается с диапазоном из текста узла: `От X до Y → цель`.
- Валидация контента гарантирует, что диапазоны покрывают `[2, 12]` без дыр.

### 6.3 Отметки и ключи

- Отметка — булев флаг в множестве `marks: Set<number>`.
- «Поставь отметку N» → идемпотентно добавляем `N`, пишем в дневник.
- «Проверь Ключ N» → читаем множество, выбираем ветку.
- «Сотри отметку N» → удаляем (например, отметка `38` — Гранат тратится).
- Особая роль: `6` — нужный ключ, `9` — исполненное желание, `38` — Гранат, `19/22/23` — ложные ключи.

### 6.4 Предметы и сокровища

- `items: Map<ItemId, ItemState>` с полями `count`, `equipped`, `effects[]`.
- Предметы могут давать постоянный модификатор к навыку (например, «+1 к Внутренней силе, пока приколот Гранат» — снимается вместе с отметкой `38` в узле `134`).

### 6.5 Временные модификаторы

Текст вида «Вычитай 1 из Энергии следующие два раза» превращается в:

```ts
type TempMod = {
  skill: SkillId;
  delta: number;
  chargesLeft: number;   // сколько применений осталось
  scope: 'next-uses';
};
```

- При каждой проверке с этим навыком модификатор применяется и `chargesLeft -= 1`.
- В статус-баре показывается бейдж: `Энергия −1 (2)`.
- Если заряды бессрочные («пока не поешь») — `chargesLeft: Infinity`, снимается событием из текста.

### 6.6 Карта и перемещение

- Сетка `cols 1–6`, `rows А–Г` = 24 клетки.
- **Соседство:** 8 направлений, включая диагональ.
- Каждый квадрат = объект `{ id, name, descriptionText, eventRefs[], neighbourIds[] }`.
- Переход «Вперёд» → экран S11 в режиме выбора → тап по соседу → «Описание местности» → указанный параграф события.
- Если параграф сам сообщает «Ты в квадрате X» — позиция обновляется автоматически, всплывает тост «📍 Квадрат 5Б».

### 6.7 Внутренняя сила (моральный слой)

- Механически — обычный навык, но UI выделяет его особо: отдельная шкала, зелёная при росте, багровая при падении.
- Все изменения логируются в дневник (S13) с причиной и узлом.
- Искупление (`300`, `455`) подаётся как отдельная значимая сцена: «Аслан прощает и укрепляет».

### 6.8 Смерть, рестарт, продолжение

- 15 мёртвых концов не дают «Game Over» в привычном смысле — сцена «пробуждения в Англии» проигрывается целиком.
- После финала: «Вернуться в Нарнию» = новая партия с Пролога.
- **Мягкий рестарт:** кнопка «Вернуться на 10 узлов назад» доступна **только** на экране финала (не во время прохождения) — чтобы неудача не была обидной, но не ломала челлендж.

### 6.9 Подсказки

- Трёхуровневая система:
  1. **Контекстная** — что означает навык/отметка/предмет (без спойлеров).
  2. **Направление** — «Возможно, стоит поискать на юге, у истока Великой Реки» (появляется после 3 подряд неудачных проверок).
  3. **Прямая** — «Нужен ключ с Глазом Осьминога» (после 15 минут блуждания без прогресса).
- Подсказки не меняют правила и не блокируются.

### 6.10 Достижения (расширение, опционально)

Не влияют на правила, только на метапрогресс:

| Достижение | Условие |
|---|---|
| Пилигрим | посетить все 24 квадрата за одну партию |
| Чистые руки | ни одного падения Внутренней силы |
| Молчаливый герой | ни разу не спеть песню |
| Три ложных ключа | подряд взять Змею, Луну и Розу |
| Возвращение | довести партию до любого финала после 3 смертей |

### 6.11 Откат на один узел

- Кнопка «‹» в заголовке S8 откатывает к предыдущему узлу, восстанавливая снимок состояния.
- Движок хранит кольцевой буфер из 20 снимков состояния (не истории текста).
- Ограничение: откат недоступен после броска — сначала нужно принять результат (защита от «переброса»). Это осознанное решение: честность кубиков важнее удобства.

---

## 7. Контент-пайплайн и модель данных

### 7.1 Принцип

`texts.md` — **единственный источник правды**. Никакого ручного переписывания контента: пишем парсер, который превращает Markdown в JSON, и валидатор, который проверяет целостность.

### 7.2 Пайплайн

```
texts.md
   │  scripts/parse-content.ts
   ▼
content/*.json   (nodes, squares, items, marks, endings)
   │  scripts/validate-content.ts   ← падает, если граф битый
   ▼
src/content/*.ts  (типизированный импорт)
   │
   ▼
Игровой движок (чистый TS, без DOM)
```

### 7.3 Схема узла

```ts
export type NodeKind = 'event' | 'location' | 'prologue' | 'ending';

export interface DiceCheck {
  skill: SkillId;                 // какой навык прибавляем
  branches: Array<{ from: number; to: number; target: NodeTarget }>;
}

export type NodeTarget =
  | { type: 'node';   id: number }        // переход на параграф
  | { type: 'map';    mode: 'forward' }   // «Вперёд»
  | { type: 'square'; id: SquareId }      // «Читай описание 6В»
  | { type: 'ending'; id: number };

export interface Choice {
  id: string;
  label: string;                  // «Спеть песню»
  target: NodeTarget;
  requires?: Condition;           // недоступный вариант
}

export type Condition =
  | { type: 'mark';   id: number; present: boolean }
  | { type: 'item';   id: string; present: boolean }
  | { type: 'skill';  skill: SkillId; op: '>=' | '<='; value: number };

export interface Node {
  id: number;
  kind: NodeKind;
  title?: string;                 // «Квадрат 3В»
  paragraphs: string[];            // текст сцены, по абзацам
  choices: Choice[];               // обычные варианты
  check?: DiceCheck;               // если есть проверка
  onEnter?: Effect[];              // отметки, предметы, навыки
  forward?: boolean;               // «Вперёд»
  ending?: EndingInfo;
  sourceRefs: number[];            // номера строк в texts.md (трассируемость)
}

export type Effect =
  | { type: 'set-mark';  id: number }
  | { type: 'clear-mark'; id: number }
  | { type: 'skill';     skill: SkillId; delta: number }
  | { type: 'temp-skill'; skill: SkillId; delta: number; charges: number | null }
  | { type: 'item';      id: string; delta: number }
  | { type: 'journal';   text: string; tone: 'good' | 'bad' | 'neutral' };
```

### 7.4 Валидатор контента (обязательные проверки)

1. Все 455 узлов (`100–554`) присутствуют, без пропусков.
2. Каждая ссылка ведёт в существующий узел / квадрат / финал.
3. Диапазоны проверок покрывают `[2, 12]` без перекрытий и дыр.
4. У каждого узла есть либо выборы, либо проверка, либо `forward`, либо финал (иначе — тупик).
5. Все используемые отметки попадают в `1–45`; все «поставленные» отметки где-то проверяются (кроме документированного резерва `3, 4, 5, 8, 17, 19–22, 35, 36, 41`).
6. Все 15 финалов достижимы обходом графа из `317`/`494`.
7. Сверка с аннотациями в `texts.md` (строки `(цели: … | кубики: … | отметки: …)`) — расхождение = ошибка парсинга.

### 7.5 Тесты «золотых путей»

Скрипты-автопроходы, прогоняемые в CI:

```ts
// Пример: путь к триумфу
script = [
  { node: 155, choice: 'courage' },
  { node: 181, forward: '2Г' },
  { node: 103, chooseKey: 'octopus' },
  // …
  { expect: { node: 235, ending: 'victory' } },
];
```

Покрываем: 1 победу, 1 падение, все 13 гибелей, и минимум 5 «длинных» маршрутов.

---

## 8. Техническая архитектура

### 8.1 Стек

| Слой | Технология | Обоснование |
|---|---|---|
| Сборка | Vite | Быстрый dev-сервер, лёгкий прод-бандл |
| Язык | TypeScript (strict) | Типобезопасный контент и движок |
| UI | Preact или React | Знакомый DX, малый вес Preact |
| Состояние | Zustand | Минимум бойлерплейта, простое автосохранение |
| 3D | three.js | Карта, кубики, сцены |
| Стили | CSS-модули + CSS-переменные | Темы, размеры шрифта, без тяжёлых UI-китов |
| PWA | vite-plugin-pwa | Офлайн, установка на домашний экран |
| Хранение | IndexedDB (idb-keyval) | Сохранения, контент, галерея |
| Тесты | Vitest + Playwright | Движок — юнит-тесты; UI — скриншоты и e2e |

### 8.2 Слои приложения

```
┌───────────────────────────────────────────────┐
│ UI-слой (Preact): S1…S15, роутер, темы        │
├───────────────────────────────────────────────┤
│ Слой сцен three.js: карта, кубики, переходы   │
├───────────────────────────────────────────────┤
│ Игровой движок (чистый TS, без DOM):          │
│   state · reducer · check · effects · conds   │
├───────────────────────────────────────────────┤
│ Контент (JSON → типизированные модули)        │
├───────────────────────────────────────────────┤
│ Персистентность: IndexedDB, миграции версий   │
└───────────────────────────────────────────────┘
```

**Ключевое правило:** движок не знает про DOM и three.js. Он принимает `GameState` и `Action`, возвращает новый `GameState` и список эффектов. Это делает его полностью тестируемым и позволяет позже переиспользовать на другом фронте.

### 8.3 three.js — где именно применяется

| Сцена | Что делаем | Бюджет |
|---|---|---|
| **Карта 6×4** | 24 плитки-меша с текстурой рельефа, орбитальная камера, подсветка активных клеток, мягкие тени | ≤ 40 draw calls, 60 fps на iPhone SE |
| **Кубики** | 2 куба с числовыми текстурами, физика (собственная простая или cannon-es), отскок, остановка | ≤ 8 draw calls, 1.4 с анимация |
| **Сцены-виньетки** | Плоские «диорамы» для ключевых локаций (Кэр-Паравел, замок Колдуньи, пещера) с параллаксом | ≤ 15 draw calls на сцену |
| **Переходы** | Растворение/наплыв между узлами через полноэкранный шейдер | 220 мс, без просадок |

**Обязательные меры для мобильных:**
- `renderer.setPixelRatio(Math.min(devicePixelRatio, 2))` — не рендерить в 3×.
- `powerPreference: 'low-power'`, пауза рендера при `document.hidden`.
- Текстуры — KTX2/Basis, ≤ 1024², ленивая загрузка по требованию.
- Общий бюджет бандла three.js — динамический импорт только при первом обращении к карте/кубикам.
- IntersectionObserver: не рендерить сцену, когда её нет на экране.

### 8.4 Фолбэк без WebGL

Если `WebGLRenderingContext` недоступен или FPS < 30 на замере — включается 2D-режим:

- Карта — CSS Grid 6×4 с SVG-иконками рельефа.
- Кубики — CSS-анимация с цифрами.
- Функциональность идентична; отличается только подача.

### 8.5 Движок: интерфейс

```ts
export interface GameState {
  runId: string;
  hero: Hero;                       // навыки, имя, тип
  square: SquareId | null;
  node: number;
  marks: number[];
  items: Record<string, number>;
  tempMods: TempMod[];
  journal: JournalEntry[];
  visitedNodes: number[];
  startedAt: number;
  history: Snapshot[];              // для отката
}

export function reduce(state: GameState, action: Action): {
  state: GameState;
  effects: Effect[];
  next?: NodeTarget;
};
```

### 8.6 Сохранения

- 3 слота + автослот.
- Формат: `{ version, state, contentVersion, checksum }`.
- Миграции: `migrations: Record<number, (s) => s>`.
- Экспорт/импорт в JSON — чтобы игрок не терял прогресс при смене устройства.

---

## 9. Пошаговый план разработки

Оценки в «человеко-днях» для одного разработчика. Фазы идут последовательно, но 4 и 5 можно частично параллелить.

### Фаза 0. Фундамент — 2 дня

**Задачи**
1. `npm create vite@latest narnia-game -- --template preact-ts`.
2. Настроить ESLint + Prettier + `tsconfig` в strict.
3. Подключить Vitest, Playwright, `vite-plugin-pwa`.
4. Структура каталогов: `src/engine`, `src/ui`, `src/three`, `src/content`, `scripts`.
5. CI (GitHub Actions): typecheck → lint → test → build.

**Готово, когда:** пустой экран собирается, тесты и линт проходят, PWA-манифест валиден.

---

### Фаза 1. Контент-пайплайн — 3 дня

**Задачи**
1. `scripts/parse-content.ts`: парсинг `texts.md` → `content/nodes.json` и `content/squares.json`.
   - распознавать заголовки `## 100…554` и `## Квадрат 1А…6Г`;
   - вытаскивать строки `(...)` с целями, кубиками, отметками;
   - отделять ветвления («Если…», «Иначе…», «От X до Y…») от повествования;
   - нормализовать кавычки, тире, пробелы.
2. Ручная вычитка: сгенерировать отчёт «узел → распознанные выборы» и сверить с книгой.
3. `scripts/validate-content.ts` со всеми проверками из §7.4.
4. Тесты контента в Vitest.

**Готово, когда:** валидатор проходит на всех 455 узлах и 24 квадратах; отчёт о расхождениях пуст; контент закоммичен.

**Риски фазы:** нерегулярные формулировки в тексте. Решение — список исключений `overrides.ts` вместо усложнения регулярок.

---

### Фаза 2. Ядро движка — 3 дня

**Задачи**
1. Типы `GameState`, `Action`, `Effect`, `Node` (§7.3, §8.5).
2. `createHero('ready' | 'custom', distribution)` + валидация распределения.
3. `rollCheck(dice, skill)`, `resolveCheck(node, result)`.
4. `applyEffects`, `evaluateCondition`.
5. `moveToSquare`, `getNeighbours` (8 направлений).
6. Журнал, снимки состояния, откат.
7. Юнит-тесты: 100% ветвей проверок, все типы условий, временные модификаторы.

**Готово, когда:** движок проигрывает полную партию «в консоли» без UI; тесты зелёные; покрытие ядра ≥ 90%.

---

### Фаза 3. Базовый UI и текст — 4 дня

**Задачи**
1. Роутер экранов S1, S2, S4, S5, S8, S15.
2. Компонент текста сцены с типографикой и маркерами изменений.
3. Панель действий и логика доступности вариантов.
4. Автосохранение в IndexedDB + восстановление.
5. Темы (пергамент / ночь) и настройка кегля.
6. E2E: пройти первые 20 узлов, закрыть и открыть вкладку, продолжить.

**Готово, когда:** играбельно текстом и кнопками на телефоне, состояние не теряется.

---

### Фаза 4. Кубики — 2 дня

**Задачи**
1. Экран S10 с пошаговым раскрытием результата.
2. three.js-сцена с двумя кубами: подброс, отскок, остановка на нужной грани.
3. Хаптика `navigator.vibrate`, звук (WebAudio, короткий сэмпл).
4. Настройки: скорость/пропуск анимации.
5. Фолбэк на CSS-анимацию без WebGL.
6. Тесты: распределение 2d6 на 100 000 бросках (χ² в допуске), корректность зажима `[2,12]`.

**Готово, когда:** бросок выглядит честно и приятно, работает на слабом Android.

---

### Фаза 5. Карта — 3 дня

**Задачи**
1. Генерация 24 плиток из `squares.json`, раскладка 6×4.
2. Орбитальная камера, зум, сброс, ограничения углов.
3. Подсветка текущего квадрата и доступных соседей.
4. Режим «Вперёд»: тап по соседу → переход к описанию местности.
5. Панель с названием и первыми строками описания.
6. 2D-фолбэк (SVG/CSS).
7. Замер производительности на iPhone SE / бюджетном Android; цель — 60 fps, ≤ 40 draw calls.

**Готово, когда:** карта открывается < 300 мс, не роняет fps, корректно выбирает соседей по диагонали.

---

### Фаза 6. Лист персонажа, предметы, отметки — 3 дня

**Задачи**
1. S12: навыки, временные модификаторы с зарядами, предметы, ключи.
2. Сетка отметок `1–45` с «где поставлена».
3. Синхронизация изменений с анимацией перелёта в статус-бар.
4. S13: дневник решений с фильтрами.
5. Тесты: изменение навыка предметом, снятие модификатора по истечении зарядов.

**Готово, когда:** все изменения прозрачны и объяснимы игроку.

---

### Фаза 7. Финалы и галерея — 2 дня

**Задачи**
1. S15 с тремя тональностями (победа / падение / пробуждение).
2. Сводка партии и «чего не хватило».
3. S7: галерея `12 / 15`, хранение между партиями.
4. Тесты: все 15 финалов достижимы скриптами из §7.5.

**Готово, когда:** каждый исход проигрывается и корректно записывается в галерею.

---

### Фаза 8. Звук, хаптика, полировка — 3 дня

**Задачи**
1. Эмбиент-подушка (море, лес, замок) с кроссфейдом между локациями.
2. Звуки интерфейса: тап, перелистывание, кубики, отметка.
3. Переходы между узлами (220 мс), анимация появления последствий.
4. Доступность: фокус, aria-live для новых сцен, контраст, динамический шрифт.
5. Тесты на реальных устройствах, отлов просадок.

**Готово, когда:** приложение звучит и ощущается цельно, доступность проверена.

---

### Фаза 9. PWA, производительность, релиз — 2 дня

**Задачи**
1. `manifest.json`, иконки, splash, `display: standalone`.
2. Service worker: precache оболочки, runtime-cache контента; офлайн-режим.
3. Lighthouse: Performance ≥ 90, PWA installable, Accessibility ≥ 95.
4. Бюджет бандла: начальный JS ≤ 150 КБ gzip (three.js — лениво, отдельным чанком).
5. Инструкция по деплою (Netlify/Vercel/GitHub Pages) и версионирование контента.

**Готово, когда:** игра устанавливается на домашний экран и работает офлайн.

---

### Сводка по срокам

| Фаза | Дни |
|---|---|
| 0. Фундамент | 2 |
| 1. Контент | 3 |
| 2. Движок | 3 |
| 3. Базовый UI | 4 |
| 4. Кубики | 2 |
| 5. Карта | 3 |
| 6. Лист/предметы | 3 |
| 7. Финалы | 2 |
| 8. Полировка | 3 |
| 9. Релиз | 2 |
| **Итого** | **27** |

---

## 10. Тестирование и приёмка

### 10.1 Уровни

| Уровень | Что проверяем | Инструмент |
|---|---|---|
| Контент | 455 узлов, 533 ссылки, 72 проверки, 15 финалов | `validate-content.ts` + Vitest |
| Движок | Все ветки проверок, условия, эффекты, откат | Vitest |
| Сценарии | Золотые пути до побед/падений | Vitest (headless-прохождение) |
| UI | Экраны, состояния, доступность | Playwright |
| Устройства | Реальные телефоны: iPhone SE, iPhone 14, Pixel 6a, бюджетный Android | Ручное + BrowserStack |
| Производительность | 60 fps карты, ≤ 300 мс открытие, ≤ 150 КБ стартовый JS | Lighthouse, DevTools |

### 10.2 Критерии готовности MVP

- [ ] Полное прохождение возможно на любом из 15 финалов.
- [ ] Ни одного битого перехода.
- [ ] Распределение 2d6 статистически корректно.
- [ ] Сохранение и продолжение работают после закрытия браузера.
- [ ] Игра работает офлайн.
- [ ] Карта: 60 fps на iPhone SE, есть 2D-фолбэк.
- [ ] Читаемо с кеглем 19 px на экране 360 px шириной.
- [ ] Все 24 квадрата и 455 узлов доступны через UI.
- [ ] Нет спойлеров в подсказках и названиях недоступных кнопок.
- [ ] Lighthouse: Performance ≥ 90, Accessibility ≥ 95.

---

## 11. Риски и юридический вопрос

### 11.1 Технические риски

| Риск | Вероятность | Митигация |
|---|---|---|
| Парсер контента ошибается на нерегулярных формулировках | Высокая | Сверка с аннотациями `texts.md` + ручной отчёт + `overrides.ts` |
| three.js тормозит на бюджетных Android | Средняя | Бюджет draw calls, DPR ≤ 2, обязательный 2D-фолбэк |
| Потеря сохранений при обновлении контента | Средняя | Версия контента + миграции + экспорт в JSON |
| Откат ломает челлендж | Низкая | Откат только назад, не после броска |
| Разрастание бандла | Средняя | Ленивая загрузка three.js и сцен |

### 11.2 Продуктовые риски

| Риск | Митигация |
|---|---|
| Игрок «теряется» в 24 квадратах | Подсказки по прогрессу, подсветка соседей, панель «где я и что ищу» |
| Нудно читать длинные параграфы на телефоне | Порционная подача, кегль и тема, оценка «времени чтения» |
| Игрок не понимает, зачем нужны отметки | Прозрачный лист персонажа + дневник |

### 11.3 Юридический вопрос (важно)

Книга-игра — **произведение Анны Шрафф** (Narnia Solo Games™, перевод Смелый Хвост), а мир Нарнии принадлежит правообладателям наследия К. С. Льюиса.

- **Для личного использования** — играбельно без ограничений.
- **Для публикации в открытом доступе** — нужно разрешение правообладателей текста и торговой марки. Без него публиковать игру с авторскими текстами нельзя.
- **Безопасный путь:** сделать движок и интерфейс универсальными (загружают любой контент в формате §7.3), а тексты подключать как пользовательские данные. Тогда распространяется движок, а не книга.

Рекомендую до релиза решить этот вопрос явно — он влияет на то, как упаковывать контент.

---

## 12. Объём MVP и версия 2.0

### MVP (фазы 0–7, ~20 дней)

- 15 экранов, текстовый движок, кубики, отметки, предметы, карта (3D + 2D-фолбэк), автосохранение, финалы и галерея.
- Без звука и эмбиента (тихий режим).
- Без достижений.

### Версия 2.0

- three.js-виньетки ключевых локаций (Кэр-Паравел, замок Белой Колдуньи, пещера с книгой).
- Эмбиент и звуковой ландшафт.
- Достижения и статистика по всем партиям.
- Режим «Хроника» — перечитать любую пройденную сцену.
- Локализация (EN) — при решении юридического вопроса.
- Синхронизация сохранений между устройствами (опционально, с бэкендом).

---

## Приложение A. Соответствие «книга → приложение»

| В книге | В приложении |
|---|---|
| Параграф «События» | Узел `Node` на экране S8 |
| «Описание местности» квадрата | Узел `kind: 'location'` + карточка-локация |
| «Вперёд» | Экран карты S11 в режиме выбора |
| «Поставь отметку N» | Эффект `set-mark` + анимация в S8 |
| «Выбери число и добавь X» | Экран S10 + пошаговое раскрытие |
| «От X до Y → NNN» | `DiceCheck.branches` |
| Лист персонажа | Экран S12 |
| Таблица случайных чисел | Кнопка броска (равнозначна, по §3 `rules.md`) |
| «Начни с Пролога» | Кнопка «Начать заново» на S15 |

## Приложение B. Быстрые решения по умолчанию

Когда нет времени на обсуждение — берём так:

1. Портрет, тёмная тема по системной.
2. Готовый герой Робин — вариант по умолчанию.
3. Кубики бросает игрок, анимация 1.2 с.
4. Откат — на 1 узел, не после броска.
5. Карта — 3D, с автоматическим 2D-фолбэком.
6. Подсказки — трёхуровневые, включены.
7. Сохранение — автослот + 3 ручных слота.
8. Контент — генерируется из `texts.md`, не переписывается руками.
